import { AnimatedPopupTriggerDirective } from './animated-popup-trigger.directive';

import {} from 'jasmine'

describe('AnimatedPopupTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedPopupTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
